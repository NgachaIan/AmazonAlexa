{
  "name": "intro-to-alexa-conversations",
  "version": "1.1.0",
  "description": "Simple Alexa Conversations example that records a color in the session and retreives it in the same session",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "zip-package": "cd .. && rm -f package.zip && zip -r package.zip *",
    "zip-lambda": "rm -f lambda.zip && zip -r lambda.zip *"
  },
  "author": "Amazon Alexa",
  "license": "ASL",
  "dependencies": {
    "ask-sdk-core": "^2.6.0",
    "ask-sdk-model": "^1.18.0"
  }
}
